Consider the additive noise SDE:

$$ dx_t = \mu(x_t) dt + \sigma dW_t$$

Corrupt it:

$$ dx_t = ((1-\lambda) \mu(x_t) - \lambda \kappa_0 x_t)   dt + ((1-\lambda) \sigma + \lambda \sigma_0 I ) dW_t$$



